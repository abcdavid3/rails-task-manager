<h1>My Tasks!</h1>
<% @tasks.each do |task| %>
  <h4><%= link_to task.title, task_path(task.id) %></h4>
  <%= link_to "Delete", task_path(task.id),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>
<h3> <%= link_to "Click to create a new task", new_path %> </h3>

